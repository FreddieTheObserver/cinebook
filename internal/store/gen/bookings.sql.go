// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.31.1
// source: bookings.sql

package gen

import (
	"context"
)

const createBooking = `-- name: CreateBooking :one
INSERT INTO bookings (ref, hold_id, showtime_id, customer_ref, total_minor, currency, idempotency_key)
VALUES ($1, $2, $3, $4, $5, $6, $7)
RETURNING id, ref, hold_id, showtime_id, customer_ref, total_minor, currency, idempotency_key, created_at
`

type CreateBookingParams struct {
	Ref            string
	HoldID         int64
	ShowtimeID     int64
	CustomerRef    string
	TotalMinor     int64
	Currency       string
	IdempotencyKey string
}

func (q *Queries) CreateBooking(ctx context.Context, arg CreateBookingParams) (Booking, error) {
	row := q.db.QueryRow(ctx, createBooking,
		arg.Ref,
		arg.HoldID,
		arg.ShowtimeID,
		arg.CustomerRef,
		arg.TotalMinor,
		arg.Currency,
		arg.IdempotencyKey,
	)
	var i Booking
	err := row.Scan(
		&i.ID,
		&i.Ref,
		&i.HoldID,
		&i.ShowtimeID,
		&i.CustomerRef,
		&i.TotalMinor,
		&i.Currency,
		&i.IdempotencyKey,
		&i.CreatedAt,
	)
	return i, err
}

const getBookingByHold = `-- name: GetBookingByHold :one
SELECT id, ref, hold_id, showtime_id, customer_ref, total_minor, currency, idempotency_key, created_at
  FROM bookings
 WHERE hold_id = $1
`

func (q *Queries) GetBookingByHold(ctx context.Context, holdID int64) (Booking, error) {
	row := q.db.QueryRow(ctx, getBookingByHold, holdID)
	var i Booking
	err := row.Scan(
		&i.ID,
		&i.Ref,
		&i.HoldID,
		&i.ShowtimeID,
		&i.CustomerRef,
		&i.TotalMinor,
		&i.Currency,
		&i.IdempotencyKey,
		&i.CreatedAt,
	)
	return i, err
}

const getBookingByIdempotencyKey = `-- name: GetBookingByIdempotencyKey :one
SELECT id, ref, hold_id, showtime_id, customer_ref, total_minor, currency, idempotency_key, created_at
  FROM bookings
 WHERE customer_ref = $1
   AND idempotency_key = $2
`

type GetBookingByIdempotencyKeyParams struct {
	CustomerRef    string
	IdempotencyKey string
}

func (q *Queries) GetBookingByIdempotencyKey(ctx context.Context, arg GetBookingByIdempotencyKeyParams) (Booking, error) {
	row := q.db.QueryRow(ctx, getBookingByIdempotencyKey, arg.CustomerRef, arg.IdempotencyKey)
	var i Booking
	err := row.Scan(
		&i.ID,
		&i.Ref,
		&i.HoldID,
		&i.ShowtimeID,
		&i.CustomerRef,
		&i.TotalMinor,
		&i.Currency,
		&i.IdempotencyKey,
		&i.CreatedAt,
	)
	return i, err
}

const getBookingByRef = `-- name: GetBookingByRef :one
SELECT id, ref, hold_id, showtime_id, customer_ref, total_minor, currency, idempotency_key, created_at
  FROM bookings
 WHERE ref = $1
`

func (q *Queries) GetBookingByRef(ctx context.Context, ref string) (Booking, error) {
	row := q.db.QueryRow(ctx, getBookingByRef, ref)
	var i Booking
	err := row.Scan(
		&i.ID,
		&i.Ref,
		&i.HoldID,
		&i.ShowtimeID,
		&i.CustomerRef,
		&i.TotalMinor,
		&i.Currency,
		&i.IdempotencyKey,
		&i.CreatedAt,
	)
	return i, err
}
